# Core dependencies
flask[async]==2.3.2
playwright==1.48.0
httpx[http2]
uvloop==0.19.0
python-dotenv==1.0.0 
# Async and performance
anyio==4.3.0
asyncio==3.4.3
# Data Handling
pydantic==1.10.9   # Data validation (if used in extended API)
orjson==3.9.5      # Faster JSON parsing (optional)
# Utilities
tqdm==4.65.0       # Progress bars for long attacks (optional)
redis==4.5.5       # For caching payloads/responses (optional)
click==8.1.3       # CLI support (if needed)

# Dev/Testing
pytest==7.4.0
pytest-asyncio==0.21.0
# Security
certifi==2024.2.2
urllib3==2.2.1

